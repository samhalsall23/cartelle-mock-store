import { IoChevronBackOutline } from "react-icons/io5";

export function CheveronLeftIcon({ className }: { className?: string }) {
  return <IoChevronBackOutline className={`w-6 h-6 ${className}`} />;
}
