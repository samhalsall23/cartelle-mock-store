import { IoChevronForwardOutline } from "react-icons/io5";

export function CheveronRightIcon({ className }: { className?: string }) {
  return <IoChevronForwardOutline className={`w-4 h-4 ${className}`} />;
}
